import PagePlaceholder from '../../components/shared/PagePlaceholder'
import { ClipboardList } from 'lucide-react'

const OrderQueue = () => {
  return <PagePlaceholder title="Order Queue" subtitle="View and manage incoming kitchen orders in real-time." icon={ClipboardList} />
}

export default OrderQueue
