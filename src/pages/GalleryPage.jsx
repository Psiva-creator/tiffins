import PagePlaceholder from '../components/shared/PagePlaceholder'
import { Images } from 'lucide-react'

const GalleryPage = () => {
  return <PagePlaceholder title="Gallery" subtitle="A visual feast — take a peek at our dishes and our restaurant." icon={Images} />
}

export default GalleryPage
