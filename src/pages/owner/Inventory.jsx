import PagePlaceholder from '../../components/shared/PagePlaceholder'
import { Package } from 'lucide-react'

const Inventory = () => {
  return <PagePlaceholder title="Inventory" subtitle="Manage stock levels, suppliers and purchase orders." icon={Package} />
}

export default Inventory
